# Hadolint — Dockerfile linting

#

# Install: https://github.com/hadolint/hadolint

# Run (when Dockerfiles exist):

# hadolint -c infrastructure/.hadolint.yaml infrastructure/Dockerfile.*

#

# CI can add a hadolint step once application Dockerfiles are introduced.
