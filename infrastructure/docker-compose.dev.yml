# Local development overrides — bind ports to 127.0.0.1 only.
# Chatwoot: chatwoot/docker-compose.vps.yml (ou rede Docker sem bind).
services:
  postgres:
    ports:
      - '127.0.0.1:9410:5432'

  frontend:
    ports:
      - '127.0.0.1:9400:3000'

  api:
    ports:
      - '127.0.0.1:9401:3000'

  ai:
    ports:
      - '127.0.0.1:9402:8000'

  n8n:
    ports:
      - '127.0.0.1:9404:5678'

  rabbitmq:
    ports:
      - '127.0.0.1:9407:15672'

  minio:
    ports:
      - '127.0.0.1:9405:9000'
      - '127.0.0.1:9406:9001'
