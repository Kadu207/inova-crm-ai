# Plan: 013-coderabbit-security

1. Spec + `.coderabbit.yaml` + `docs/security/coderabbit.md`
2. Nest hardening (helmet, throttler, swagger, filter)
3. Next headers/CSP
4. Gitleaks CI + gate artifacts + baseline
5. `npm run gate` PASS; deploy API/FE
