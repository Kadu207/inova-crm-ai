# Plan: 012-company-contact-create

## Fases

1. `CompaniesClient` + `CompanyCreateModal` + detail
2. `ContactsClient` + `ContactCreateModal` + detail (companyId select opcional)
3. Trocar pages `empresas` / `contatos`
4. Docs/baseline

## Gate

`npm run gate` PASS
